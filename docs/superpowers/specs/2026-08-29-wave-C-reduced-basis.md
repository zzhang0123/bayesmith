# 执行页 Wave C / `reduced_basis` —— 半切,以及一次「找全了」错三遍

> **文档状态：`record`** · 已落地批次/审计/测量的历史记录，写作当天为真，非当前权威。索引见 docs/README.md。

> 计划:§五 Wave C 第三行;裁决 **D60**(新)、**D41**、**D44**、**D12**、**B11**。
> 前一批次:`2026-08-29-wave-C-npe-opening.md`。
> **日期**:2026-08-29 · **本页状态:开波、验收、切换、变异都已做。**
> 上游提交:e-RHINO `eb747fa`。

## 〇、这一行我先后给过三个答案,前两个是错的

| 时点 | 结论 | 错因 |
|---|---|---|
| 一 | 「可以切」 | **没查 §四 栏位**——这正是 D58 那次的错 |
| 二 | 「§4.3 说不迁移,别切」 | **没读推翻链**:§五 把 `reduced_basis` 与 `calibrate`、`npe` **写在同一行** |
| 三 | 「G4 只做 5/8,本行被卡住」 | **G4 页自己写着**「三者…**跟 Wave C 走**」——**同波工作,不是前置** |

第三个答案是一次对抗式 workflow 给的,**而它也错**。**结论正确不等于依据正确**:
它对 G4 与 sqrtinfo 的取证都是对的,唯独把「跟 Wave C 走」读成了「挡住 Wave C」。
**我照样得自己去读那一页。**

## 一、真正的形状,由既有判据读出

**不是新造的判据**:D42 判 `simulate_pairs` 用的那一把尺,原样量这十个名字。

| 名字 | 判定 | 实测签名 |
|---|---|---|
| `orthonormal_transform`、`orthonormalise`、`numerical_rank`、`select_svd`、`select_greedy` | **委托** | 只收数组 |
| `score_directions` | 留守 | 收 `space` + `pipeline` + `state_template` |
| `build_reduced_basis` | 留守 | 收 pipeline/`State`/`NoiseModel`,**产出容器** |
| `basis_fidelity` | 留守 | 只读写两个 D12 容器 |
| `ReducedBasis`、`FidelityReport` | 留守 | D12 |

**所以 G4 做的正是该做的那一半。** 半切先例:Wave A 的 `numpyro_bridge` 与
`uncertainty`——不进 `SWITCHED`,cross-check 保留。

## 二、验收:五个全部逐位相同(x64,删除之前)

`max|Δ| = 0.0`,含 `orthonormal_transform` **两个返回元素**都是。签名逐字符相同。
**删完之后取不到**——这条教训现在每一页都写着,而这一页是第三次照它办事。

## 三、两条新拒绝随委托到达,按铁律 3 声明

1. **D41 的 float32 拒绝**(远端每入口都拒;近端不拒,它住 x64)。
   **实测安全**:x64 之外对本模块的引用**全是散文**,没有一处调用。
2. 远端 `_rows` 的二维形状拒绝。

**近端十六条拒绝无一属于这五个**,所以委托**不丢失任何近端拒绝**。

## 四、接缝变异:5 条 5 杀(x64 会话内)

不再复述;要点是 M5(`select_greedy` 改调 `select_svd`)被
`test_greedy_selection_spans_the_bank_to_a_declared_tolerance` 杀——
**两个选择器的差别是被测着的**,这不显然。

## 五、真正贵的那一段:八处按行号的引用,而「找全了」错了三遍

`reduced_basis.py:171-180` 被 `src/` 与 `tests/` **按行号引用八次**,全都指同一个
置换 bug(jacobian 的 key 序对上声明序)。**没有任何东西校验那个行号还指着对的代码。**
而委托的五个里有两个排在它前面,所以这次切换把它从 **171-180 挪到 159-168**。

**找全它们错了三遍,每一遍我都以为找全了:**

| 轮 | 做法 | 结果 |
|---|---|---|
| 1 | `grep --include='*.py' '171-180'` | 6 处,以为齐了 |
| 2 | 放宽到 `*.md` | **还有 3 处没改** |
| 3 | 按**内容**做 old→new 映射 | 引用共有**五种写法**(`:114`、`:114-119`、`:114-120`、`:164`、`:171-180`) |

**这是本会话同一个病的第四次**(私名普查的假阳性、漏掉大写的 D10、§4.3 漏掉
一整条、以及这次)。形状每次相同:**匹配器只认一种写法,其余读成「不存在」,
而「不存在」读起来像「查过了」。**

**处置**:八处**活的**引用全部更新;`docs/superpowers/` 下**历史文档不动**——
一份写了日期的计划说过的话就是它说过的话,**改里面的行号是篡改记录不是维护它**。

**并加了守卫** `tests/test_line_citations.py`,两半:
* 每处引用的范围**仍含它声称的东西**;
* **扫描到的每一处引用都必须在表里**——因为这个文件存在的理由,正是一次 grep
  漏掉了两种写法。没有这后一半,表只守着写它时碰巧存在的那几种拼法。

外加一条反空洞:旧范围 `171-180` **必须不再**含那段注释,否则本文件的前提就是假的。

## 六、留给下一位

**Wave C 三行(`calibrate`、`npe`、`reduced_basis`)已全部落地。** 下一波是
**Wave D**:`chain` + 证据族**七**模块(八减去本行)。先决 D12+G3+G6。
**D44 的 `compress_reduced_basis` 归 Wave C**,但它要 `build_reduced_basis` 的
远端对应物——而那一个按本页判定**留守**,所以 **D44 的前提需要重谈**:
它假设三个名字会过去,而实测判定是它们不过去。**这是一条真的待办,不是我漏做的。**
