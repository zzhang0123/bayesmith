# R1 close-out — 冻结 artifact 协议与 provenance 基线

> **文档状态：`record`** · 已落地批次/审计/测量的历史记录，写作当天为真，非当前权威。索引见 docs/README.md。

**日期：** 2026-09-01

**设计依据：** [Bayesmith 顶层设计](2026-08-30-bayesmith-top-level-design.md) §2、§4、§8 R1；执行计划 [R1 计划](../plans/2026-08-30-r1-task-artifact-provenance.md)。

**被验收的代码基线：** Task 1–7 由会话 B 提交（HEAD `cfd17e5` 之前），Task 8 提交 `876cdac`（发布 artifact 协议），Task 9 即本页加两处测试路径修正（见下）。

**结论：** R1 closed。八个完成定义逐条有本次实测证据；source full suite、ruff、built-wheel suite 与 rheplicant consumer gate 全绿。

---

## 验收矩阵

逐项门槛与本次实测。没有一项数字复用 R0 或更早的绿色结论。

| 门槛 | 要求 | measured |
|---|---|---|
| 统一协议 | analytic/exact draw、MCMC、optimization 都返回统一协议 | `execute_task` 对 gcr/snis/NUTS/MAP 全返回 typed Result，`tests/dispatch/test_task_execution.py` 25 项 |
| byte-stable round-trip | 同一 artifact byte-stable round-trip | `tests/artifacts/test_codec.py` 16 项 + `tests/test_public_api.py::TestArtifactPersistence` 6 项 |
| 失效矩阵 | model/graph/data/task/compilation/evaluation 按矩阵失效 | `tests/artifacts/test_identity.py` 58 项表驱动 + boundary mutant |
| Refusal structured | Refusal 只读 structured fields，不解析 exception string | `tests/artifacts/test_refusal.py` 21 项 + `tests/dispatch/test_task_protocol.py` adapter 测试 |
| 五进五出一对一 | 五类 Task→Result 穷尽一对一，PredictiveResult 非隐式组合 | `tests/artifacts/test_tasks_results.py` 97 项（含 mapping 双向相等断言与 PredictiveResult 独立 round-trip） |
| gate 两轴 | 状态/verdict 两轴，truth table 与 permutation invariance 穷尽 | `tests/artifacts/test_gates.py` 58 项 |
| 数值 parity | 新旧执行数组逐字段数值一致 | `tests/dispatch/test_task_execution.py` 25 项 `assert_allclose` + 逐字段 |
| sibling consumer | sibling consumer 无回归 | 见下 consumer gate：inference 1115 passed、seam 31 passed + 1 xfail，exit 0 |

---

## 完成门槛实测记录

### Source full suite（§9.2）

```
.venv/bin/python -m pytest -n 4 --junit-xml=runs/<run>/junit.xml
```

| 项 | 值 |
|---|---|
| JUnit | 5342 tests、0 failures、0 errors、0 skipped |
| 独立 exit | 0 |
| 用时 | 1193.60 s（0:19:53） |

计数从 JUnit 读，不取 `-qq` 消失的终端摘要。

### Lint（§9.3）

```
.venv/bin/ruff check src/ tests/
```

`All checks passed!`，exit 0。未运行 `ruff format`。

### Built wheel 与 wheel suite（§9.4）

```
.venv/bin/python -m build --outdir <build>
python3 -m venv <build>/venv
uv pip install --python <build>/venv/bin/python <build>/bayesmith-*.whl pytest pytest-xdist hypothesis
(cd /tmp && <build>/venv/bin/python -c 'import bayesmith; print(bayesmith.__file__)')
(cd /tmp && <build>/venv/bin/python -m pytest /Users/zzhang/projects/bayesmith/tests -n 4 --junit-xml=<build>/wheel.junit.xml)
```

| 项 | 值 |
|---|---|
| build 产物 | `bayesmith-0.6.2.tar.gz`、`bayesmith-0.6.2-py3-none-any.whl` |
| wheel 身份 | `<build>/venv/lib/python3.12/site-packages/bayesmith/__init__.py`（site-packages，非 source-tree） |
| wheel suite JUnit | 5342 tests、0 failures、0 errors、105 skipped |
| 独立 exit | 0 |
| 用时 | 1149.61 s（0:19:09）；5237 passed + 105 skipped |

105 个 skip 全部是 `tests/crosscheck/`：wheel venv 未安装 sibling `rheplicant`，那些 seam 测试按 `importorskip` 让位，是预期的 skip 而非失败。

**第一次 wheel run 作废，原因已修并重跑。** 它报了 2 个 failure，都是 Task 8 加的两个文档 guard：
`test_the_artifact_docs_pin_the_five_results_and_grounds` 与
`test_the_readme_workflow_mentions_the_typed_round_trip` 用 cwd-relative
`pathlib.Path("docs/artifacts.md")`/`("README.md")`，在 `cd /tmp` 的 wheel run 里找不到文件。
修复：改用 `Path(__file__).resolve().parent.parent` 锚定仓库根（与 `tests/test_readme_count.py` 同法）。
这是 test-only 修改，不增删测试、不动 collection 数（仍 5342），也不改任何 `bayesmith` 源码。
重跑 wheel suite 得上面的绿结果。

### rheplicant consumer gate（§9.5）

sibling checkout `/Users/zzhang/projects/e-RHINO`，revision `27e621b98b6b4cd853acf84f30a0d65555c9ac76`，
其环境导入 `/Users/zzhang/projects/bayesmith/src/bayesmith/__init__.py`。
sibling 有一项未提交改动：`src/rheplicant/inference/chain.py`（+26/−9，把 `smooth()` 委托给
`bayesmith.marginal.chain.smooth`）。**未清理、未修改它**；R1 从始至终不碰 `marginal/chain.py`，
Task 6/7 只加 `dispatch/task.py` 适配器、不改数值内核，所以该 dirty 状态不影响本 gate 的预期。

| Suite | 结果 | exit |
|---|---|---:|
| `tests/inference -n 4` | 1115 passed、0 skipped/failed/error，92.09 s | 0 |
| `JAX_ENABLE_X64=1 tests/seam` | 31 passed、1 xfailed、0 failed/error，52.90 s | 0 |

JUnit 逐 suite：inference `{tests:1115, failures:0, errors:0, skipped:0}`；
seam `{tests:32, failures:0, errors:0, skipped:1}`（skipped 即那 1 个 xfail）。

---

## 完成定义 §11 逐条

| # | 完成定义 | 证据 |
|---|---------|------|
| 1 | 五 Task、五 Result、四 posterior representation、RunRecord 安全 round-trip | `tests/artifacts/test_tasks_results.py`（97）对每类构造最小合法对象并 round-trip；`test_codec.py`（16）钉 byte-stable/dtype/shape/bytes；`test_base.py` 钉 NamedArray/RunRecord 不变量；`TestArtifactPersistence`（6）钉文件 round-trip |
| 2 | 映射穷尽一对一，PredictiveResult 非隐式组合 | `test_tasks_results.py` 断言 `set(PRIMARY_RESULT_BY_TASK)==set(TaskKind)`、`set(values())==set(ResultKind)`、`len(set(values()))==len(ResultKind)`，并有 PredictiveResult 独立 round-trip |
| 3 | `Refusal.grounds` 冻结，不解析 exception string | `test_refusal.py`（21）断言 `grounds` 在 fields 且 `evidence` 不在；`tests/dispatch/test_task_protocol.py`（36）adapter 只读 `NotGaussian.reason`/`NotLogLinear.reason`/MAP verdict 结构化字段 |
| 4 | fingerprint 粒度与失效矩阵有 boundary mutant tests | `test_identity.py`（58）表驱动失效矩阵 + mutant：翻转 array 一 byte 改 data digest、progress label 不改 task digest、mapping 顺序不改 digest、`repr` 含地址拒绝、只改 evaluation threshold 不使 Result 失效 |
| 5 | gate 两轴 + truth table + permutation invariance 穷尽 | `test_gates.py`（58）穷尽 applicability×conclusion 组合、§0.6 真值表、slot permutation 不变、FAIL 优先于 ABSTAIN |
| 6 | 新旧入口逐字段数值 parity | `test_task_execution.py`（25）gcr / weighted gcr+snis / nuts / estimate / MAP 逐 latent `assert_allclose` + method/weights/ESS/khat/unreliable/residual/iterations/objective 逐字段一致，相同 key 只各跑一次不额外 split |
| 7 | legacy API / source full suite / ruff / wheel suite / consumer gate 全绿 | source 5342/0/0/0 exit 0；`ruff check src/ tests/` 全过；wheel 5342/0/0/105 exit 0；consumer inference 1115 + seam 31/1xfail 均 exit 0；legacy `Posterior`/`Estimate` identity 由 `tests/test_public_api.py` 与 `tests/dispatch` 钉住 |
| 8 | close-out 用本次真实 SHA/JUnit/consumer revision | 本页 SHA `876cdac`（代码）+ 本提交（文档）；JUnit 与 revision 均为本次实测，见上 |

---

## 关闭判决

八个完成定义、验收矩阵八行、source/lint/wheel/consumer 四个 gate 均有本次实测证据。R1 closed；
下一步是 R2/R4 接 Evidence/Predictive/Simulation 的运行时执行与 R3 的 workflow/gate 消费。
任何后续代码变更都会产生新的 candidate，需要重新跑相应 gate，不能借用本页的绿色结论。
